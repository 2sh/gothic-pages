<%
const notesString = JSON.stringify(typeof notes !== 'undefined' ? notes : {})
-%>
<i-line
  id="L<%=id%>"
  x-data='{ id: <%=id%>, text: "<%=text%>", notes: <%=notesString%> }'
  @click='info = { id, text, notes }'
  :class="{ 'line-selected': info && info.id == id }"
  ><%=text%></i-line>